---
title: "20230103西安出差日志"
draft: false
section: "work"
sourcePath: "markdown/work/dailylog/2023/20230103西安出差日志.md"
slug: "work/dailylog/2023/20230103西安出差日志"
---

# 20230103 出差日志

于20230105周四下午到达西安。

## 问题寻导日志

西安出差：
1月3日 - 1月19日

1.5 日 577.5
1.5-1.12 2280
1.12-1.17 1075
1.17 258
1.18 319.24
4509.74 + 15*180 = 7209.74
住宿共 3931.24
1285元 机票
取 1020 元报销

[2.1]
搭建环境的必要条件是什么样子的，我们是否可以搭建这个环境。
在搭建环境之前先进行线程池测试。
如何搭建这样的环境，server 和 client 模式，是否能够通过两个板子来实现模拟。

关键点：
1. PCIe 通信开发缺少 Demo 样例，无法确定需要的资料方向，无法估计后续开发耗时；

需要的额外支持：
1. 环境是否能够搭建；

开发过程分为这么几个阶段：
1. 给出远程 sqlite3 接口； - 2
2. 编写 Demo 进行地址级别的通信，在环境具备前只需要大概模板； - 3 - 需要调研
3. 性能优化； - x
4. 编写网络测试代码,实机/仿真环境测试； - 4

部分对接信息Q&A：
1. PCIe 是否有接口可以给我们进行使用？
   wxh:
   "PCIe没有啥接口，cpu之间通过共享内存进行交互，就是一段可以访问的地址空间"
    wxh:
    没有接口，地址空间是固定的，直接读写
    wxh:
    就是类似 *(uint *)0x40001234 这样
2. 我们想我们这边能否通过用两个板子来搭建类似咱实际的这种两个CPU通信的环境？
    wxh:
    我们这目前就是一个开发版和一个产品
    wxh:
    这个我不知道，都有pcie应该可以吧，可能得用转接线吧
3.  后续 PCIe 接口通信的开发我们还是主要参考之前的《FT-2000-4_软件编程手册-v1.3-20200226》这个文档是吗，根据其中的地址空间划分和寄存器进行开发？
   wxh:
    是的

主要是我们想要了解一下我们这边开发 PCIe 相关接口的量。
按照地址空间来的话，还有很多问题。
我们是否需要考虑到串口级别开发，还是说我们直接就是往地址空间里面读写数据，然后判断是否有数据这个我们是不是还得看控制寄存器中的上升沿之类的


[01.05]
下午3点到西安，到 631 进行简单对接，确定：
1. 631 可以提供 PCIe 文件系统接口；
2. 使用数据库本地接口，需要开发类似 sqlite3 的部分接口；
3. 确定当前业务流程中用到的 sqlite 接口，明确诸如预处理的 sql 语句执行方式不可用，并且部分 sql 语句无法支持。


[1.6]周五
上午到 631 ，进一步收集接口使用。----其实没必要来，昨天已经收集完成
下午留在酒店进行接口开发，两个思路，一个是贴近内核和sqlengine 接口，一个是使用远程接口进行修改。
确定用思登的 内核开发。

[1.7]
周末进行接口开发实现如下接口：

```C++
int sqlite3_open_v2(const char* filename, /* Database filename (UTF-8) */
    sqlite3** ppDb, /* OUT: SQLite db handle */
    int flags, /* Flags */
    const char* zVfs /* Name of VFS module to use */
);
int sqlite3_close_v2(sqlite3*);


int sqlite3_get_table(
    sqlite3* db,          /* An open database */
    const char* zSql,     /* SQL to be evaluated */
    char*** pazResult,    /* Results of the query */
    int* pnRow,           /* Number of result rows written here */
    int* pnColumn,        /* Number of result columns written here */
    char** pzErrmsg       /* Error msg written here */
);
void sqlite3_free_table(char** result);

int sqlite3_exec(
    sqlite3*, /* An open database */
    const char* sql, /* SQL to be evaluated */
    int (*callback)(void*, int, char**, char**), /* Callback function */
    void*, /* 1st argument to callback */
    char** errmsg /* Error msg written here */
);

void sqlite3_free(void*);
```

编写基本的测试用例，建表语句如下：

```bash
open 'test';
create table t1(id int,name string);
insert into t1 values(1,'name1');
select * from t1;
``

[1.9]周一
开始对接，替换源码中的文件接口为 631 提供的文件系统接口。
并且开始在 631 搭建的仿真环境进行 基本业务 测试。
调整接口的使用，因为 DBFile 类接口与文件系统接口有部分同名，导致调整时间较长。

下午，发现打开数据库后无法找到表。

[1.10]
继续进行调试，在天脉上建表，能够找到。
宇鑫提出是因为数据库位数问题，解决。

[1.11]
发现数据库查询数据时出错的问题。
发现是数据库调用接口出错，getPos 定位数据出错

发现数据库目前会停止到 new 时。猜测是系统级内存使用完毕。

## 总结

现阶段工作结论：
1. 631 方目前业务 sqlite 业务接口可以使用；

后续待解决问题：
2. 无法配合的 sql 语法 ， limit 、 join 、ignore ，字符串的各种使用，例如使用 string 作为主键等？？//思登补充
3. 预处理和回调，可能后续性能需要使用，S 需求，性能方还包括连表查询；
4. 掉电问题；
5. 存储引擎；
4. PCIe 文件系统需要我们自己开发；

先阶段业务：
1. 接收存储业务通过；
2. 故障业务，遇到问题；第三个业务还没有进行到；
3. 性能要求严格，并未测试，随机查询，当前业务是只查询最新的；

性能方面，可完善点：
1. 内存管理模块覆盖率低；
2. 对比稳定版本，主要是通过 sql 语句进行操作数据库；

时间：
1. S0 阶段，联试，大概 4 月初，需要等 631 产品进度，拿到 S1 阶段需求；
2. 希望能够拿出一个结论，何时演示；
3. 适配接口的问题，第一阶段，第二阶段；
4. 性能何时解决；

## 附录

### 文件系统接口修改笔记

::open
BaseFile_TM.cpp      168    331  

::fstat
BaseFile_TM.cpp      183    187  238

::unlink
BaseFile_TM.cpp      180

::stat
BaseFile_TM.cpp      155

::close

::ftruncate
BaseFile_TM.cpp      45   196   416

#define  TM_OPEN    ::open
#define  TM_FSTAT   ::fstat
#define  TM_UNLINK  ::unlink
#define  TM_STAT    ::stat

extern int close(int fd);
extern int closedir(DIR *dirp);
extern int creat(const char *path,mode_t mode);
extern int ioctl(int fd,int command,void *buffer);
extern off_t lseek(int fd, off_t offset,int whence);
extern int mkdir(const char *path, mode_t mode);
extern int open (const char *file, int flags, ...);
extern DIR *opendir(const char *dirname);
extern ssize_t read(int fd,void *buffer,size_t count);
extern struct dirent *readdir(DIR *dirp);
extern int readdir_r(DIR *dirp, struct dirent *entry, struct dirent **result);
extern void rewinddir(DIR *dirp);
extern int rmdir(const char *path );
extern int unlink(const char *file);
extern ssize_t write(int fd,const void  *buffer,size_t count);
extern int utime(char *file,struct utimbuf * newTimes);
extern char* getcwd(char *buf,size_t size);
extern int chdir(const char *path);
extern int fcntl (int, int, ...);
extern int dup( int fd );
extern int dup2( int fd1, int fd2 );
extern int remove(const char *filename );
