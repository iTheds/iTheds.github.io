---
title: "0419work_data"
date: "2021-4-11"
subtitle: "咱们中国什么时候也能搞出高性能芯片"
author: "Lonnie iTheds"
tags:
  - work
draft: false
section: "work"
sourcePath: "markdown/work/dailylog/2021/0419work_data.md"
slug: "work/dailylog/2021/0419work_data"
---
# 0419work_data

汇报人： 严林涛
汇报时间： 2021年4月24日

## 本周目标

完成大体，周三前分发模块。

## 工作内容

1. 功能测试
    - [x] wsa事件绑定测试
    - [x] 线程通信测试
    分发模块不适用分发线程进行发送，直接在次级主线程中发送。

2. 整体代码合并
    - [x] 整体代码合并

## 每日进程

周一：
1. 编写TCP分发模块，拟定使用事件监听的方案进行。
2. 对WSA事件进行了解，并初步编写功能性测试代码。

周二：
1. 了解线程间通信的方法，提出解决方案。
2. 根据周一功能测试，编写分发模块框架以及大体内容。模块测试完毕。

周三：
1. 测试线程间通信的问题，疑似存在读取冲突。总是感觉有接收遗失在里面。解决该问题，直接在次级线程中分发。
2. 整个程序合并，并且进行测试，初步无误。将剩余三个参数文件合并。

周四：
1. 主程序进行调试。
2. 程序考虑封装问题，函数名称动态化。
3. (晓康)编写上端发送数据进行计数。使其可输出到文件进行验证。
4. (金雨)编写下端接收数据Demo端口测试。使其可输出到文件进行验证。
5. (晓康)编写格外的程序打开数据库。

周五：
1. 分发模块UDP测试并优化。
2. 内存空间整理，主要是malloc，和缓冲区。
3. 规范化，<>待解决事件解决。
4. 内存整理，查看内存数据归属，分配内存，释放内存。
5. 测试模块抽出。以返回错误码的方式代替输出信息。

## 测试

1. 额外的读取文件程序，输出信息，并且统计条数。格式：
```C++
------|-----------|--------|--------
AIS | 3 | !AIVDM,1,1,,B,H68s6M@446222222222222222200,0*1D
```


2. 上端发送数据改进。显示buffer，并且显示条数。
```C++
------|-----------|--------|--------
AIS | 3 | !AIVDM,1,1,,B,H68s6M@446222222222222222200,0*1D
```

	_vhw,
	_mwv,
	_dbt,

	_plot,
	_track,

## 日志

1. 用到了inet_addr()函数，关闭了SDL检查。

2. dbt数据，单位数据是否需要存储。

3. 下端接收数据时，是否也是6tcp，2udp。

4. 用于交接的三个数据都没有准确的格式表，需要拟制作一张。

5. 两个UDP发送的数据在上端存在问题。

6. 向上连接TCP时，一旦断开无法重连。

7. TCP发送模块，事件绑定后，有端口有问题，未输出任何提示。

## 后续优化

 - [x] 数据库的名称进行动态生成，格式：
    数据项_时间

 - [x] 端口需要提出

 - [ ] 关于接收后程序在向后执行的时间内，可能存在错过接收的可能性。recv函数是会有一个存储的过程的。应该是不会产生这种情况。

## 后续工作

1. 整体数据进行比对测试。
2. 两个UDP数据，疑似在上端发送时存在问题，无法进行详细测试。
3. 向上连接TCP时，一旦断开无法重连。
4. TCP发送模块，事件绑定后，有端口有问题，未输出任何提示。

## 项目文档

插入数据和建表已经依赖于db code
 , 插入数据失败
 , 建表失败

创建线程
td[i].datatype , 创建某个主线程失败

-3 , 创建socket失败

rev
-4 , TCP接收数据失败，疑似连接断开，并且重连失败
-5 , 接收模块UDP初始化bind失败

分发线程
-6 , 线程过多
-8 , 接收连接失败

-1 , 通用报错

3 , 标识信息
4 , 汇报记录

## 分发模块测试

add
test1
9000
192.168.1.145
tmp
add
test2
9001
192.168.1.145
tmp
add
test3
9003
192.168.1.145
tmp
add
test4
9003
134.168.1.145

010100110100100101000001
010100110101000001000111
010101000100010001001000

010101110100100001010110
010101100101011101001101
010101000100001001000100

010011110100110001010000
010000010101001001010100

## 自主扩展

 - [ ] RUST语言所衍生的架构有哪些

是否可以生成某些库
对应windows、linux、天脉等操作系统，着重国产操作系统的兼容性。

## 关于报销

粘贴要粘贴左上角(横板)
时间错误
费用报销审批单最下面
小票不重叠