---
title: "内蒙出差"
draft: false
section: "work"
sourcePath: "markdown/work/distributedNet/内蒙出差.md"
slug: "work/distributedNet/内蒙出差"
---

# 出差日志 - 内蒙古[2024.11.21 - 2024.12.10]

## 出差目标

* 完成在实际环境中的演示验证。

在出差前，已完成所内自测，基本能够展示指标要求。

## 总体意见

记录从 12月8日 给总体曹东旭展示成果，软件能够顺利通过金美的接口构建完整的分布式网络。
但是曹东旭觉得与预期不符，由于金美软件接口只体现了交换机节点，未能体现出网络中的主机节点内容，未达到展示效果。
结论：项目基本完成，能够进行指标验收，不收录到总体测试大纲。

追溯未收录到总体测试大纲原因：
1. 主要原因，**`不具备演示效果`**：
   1. 由于使用的是金美接口，未具体展示实际场景下的设备信息；
   2. 无法展示所有的工作内容，网络图谱太过于简陋；
   3. 功能性缺失；未展示所有图谱；
   4. 展示问题；未对体系功能进行展示；
2. 其他原因：
   1. 总体的测试大纲中，分承合同功能并非强制要求收录；
   2. 课题贴近的金美软件依赖，并没有在 大专项 中着重使用；
   3. 在出差之前，已经不准备将分承合同收录到测试大纲；

后续意见：

1. 建议后续的，课题尽可能跟实际使用的课题贴近，比如和总体系统界面具备交互；金美软件的使用频率并不高，并且其提供的信息有限；
2. 之后采用更真实的环境进行集成；
3. 界面优化；
4. 在实际环境中完成对接底层的金美软件，是有一定意义的；

## 收获

1. 对 docker 已经十分熟练，包括使用其进行单节点运行，多个节点部署， docker 网络部署，配置 docker 开发环境等；
2. 软件开发中：
   1. 采用组合模式和工厂模式，有效使得软件架构具有极大易扩展性；但是，对于 C++ 中的特性扩展仍然存在菱形继承问题，还需要进行实践；并且对 QT 的前端，应该采取何种开发模式并不熟悉；
   2. 网络图谱，直接采用邻接矩阵的方式进行通信信息交换，即采用[节点序列，邻接矩阵]来描述点和边，固然节省空间，但是未携带足够的连接信息；而且其最大的问题是，不具备良性操作方式，如果采用的是树形结构，或者一开始就使用图形结构进行存储，将对软件运算有极大提升 -- tips:至少，有利于画出尽可能少交叉点的网络拓扑图；
3. 其他；

## 开发日记

[11.21]

当天晚上到达宾馆，晚上上车开始调试，在所内测试效果，已经在车上完整达到。
提出两个需求，准备开发新功能：
1. 网络拓扑图；
2. 多跳节点的到达。

[11.22-11.24]

完成 docker 上搭建开发环境，熟练使用 docker。

开发新功能：
1. `[拓扑界面功能]`。
具备基本的显示功能，但是未过滤标签节点，存在 bug ：
1. 渲染的图片位置计算有问题；

[11.25-11.27]

*`对接金美软件，适配其接口`*：
1. 发现模拟接口有些许不符合实际接口部分，进行修正； - 煊炜修改
2. 部分参数未按照手册编写，修改部分参数变量；
3. 需要记录并且完成对金美数据的回放；
4. 并且经过沟通，发现使用的接口有限，只能使用第一个接口获得图谱，并且定时获取来检测下线offline和上线的节点；

[11.28]

完善新功能：
1. 网络拓扑完善，过滤标签节点，调整界面；

开发新功能：
1. `[数据回放功能]`制作 server ，对金美的 http 协议 json 文件进行记录，并且回放；
2. `[网络拓扑更新]`对图谱进行开发，采用临时策略，依赖测试组件中的 `[节点-目标]`结构，进行图谱的更新和断联； - TODO:后续可优化为单节点订阅 http 接口进行更新；

[11.29-12.01]

修复 bug :
1. 修复手动 remove 的 bug;
2. 修复数据显示不全面的 bug;
3. 修改部分界面展示；
4. 修正[网络拓扑更新]中，反复对既定节点初始化的 bug ;

[12.01-12.3]

开发新功能：
1. [多跳节点的通信功能];作为临时策略，使用网络层而非传输层实现，在 MonitoredTask 中负载路由信息，每个节点收到路由信息都进行传递；
2. 开发图谱的路径计算方式；
3. 将二跳通信功能嵌入已有框架；
4. 调整界面；
5. 修复部分bug;

[12.04-12.07]

修复 bug ：
1. 对 qt 界面部分加入大量容错代码；
2. 对 现有的 C++ 组件加入新的容错代码；
3. 对 io 模型进行测试；

解决恶性 bug :
1. 到目前为止，还有的问题就是关闭连接之后， io model 进行 remove 时，一旦又建立了新的连接，其就会定位到之前的连接，然后一直进行循环处理。 -[x] 解决怀疑是 io 模型在处理连接关闭的候，未对操作符进行移除。

*`完成阶段性工作`*：
1. 大部分要求功能已经实现，确保软件正常运行期间不出现 bug 。
2. 与二院吴鼎展示目前效果；
3. 基本对完软件指标；

[12.08]

给总体曹东旭展示成果，能够顺利通过金美的接口构建完整的分布式网络。
但是曹博觉得与预期不符，由于金美软件接口只体现了交换机节点，未能体现出网络中的主机节点内容。
结论：项目基本完成，能够进行指标验收，不收录到总体测试大纲。

追溯原因：
1. 本质是软件自动化不够；无法全面展示所研发的内容；
2. 界面太差；无法展示所有的工作内容，网络图谱太过于简陋；
3. 功能性缺失；未展示所有图谱；
4. 展示问题；未对体系功能进行展示。

[12.09]
完善测试大纲和测试报告；
准备次日回北京。

# END OF

by iTheds


