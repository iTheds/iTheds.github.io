---
title: "0425work_data"
date: "2021-4-25"
subtitle: "0425work_data"
author: "Lonnie iTheds"
tags:
  - work
draft: false
section: "work"
sourcePath: "markdown/work/dailylog/2021/0425work_data.md"
slug: "work/dailylog/2021/0425work_data"
---
# 0425work_data

## 工作内容

## 每日日志

UDP只需要一个端口。
效率很重要，时间是其次。基础是效率很重要。

周日：
1. 测试UDP数据发送。实现使用一个端口对8个主机进行发送数据。但是存在不能退出的情况，因为UDP发送中客户端不需要向服务端进行反馈。

周一：
1. 测试所有模块。初步无误，但是数据需要重新梳理。
2. 完善额外验证数据库文件程序。

周二：
1. 更改需求，UDP发送到8个端口，直接发送方式。
2. 与雷达 对接测试组对接测试，初步无误。需要后续本组测试数据库中的内容。

周三：
1. 学习B数，并且试图理解数据库项目中代码。
2. 学习AES加密，试图理解数据库项目中代码。
3. 进行对接，在测试组中部署相关环境。

周四：
1. 增加包验证机制。在发送buffer中的前4个字节中存储包数。并且将数据存储到文件中。测试模块，读取文件效率低。
2. 发现strcat()引起的截断。使用异或方式。
3. 学习原码和补码，编写char[4]与int的转换。
4. 仍然存在截断。后续考虑使用标识符切割方式。或者有更高效的方式。暂时使用循环数据的方式。
5. 后续工作：提出完善产品方案，后续计划增加log日志文件。预计优化错误处理。

周五：
1. 对所有代码进行复盘。
2. 进行测试改善。

## 日志

1. 用到了inet_addr()函数，关闭了SDL检查。

2. dbt数据，单位数据是否需要存储。

3. 下端接收数据时，是否也是6tcp，2udp。

4. 用于交接的三个数据都没有准确的格式表，需要拟制作一张。

5. 两个UDP发送的数据在上端存在问题。

6. 向上连接TCP时，一旦断开无法重连。
    - [x] 完成
7. TCP发送模块，事件绑定后，有端口有问题，未输出任何提示。
    - [x] 完成，输出到控制台，可以考虑输出到日志logs文件。

## 后续优化

 - [x] 数据库的名称进行动态生成，格式：
    数据项_时间

 - [x] 端口需要提出

 - [ ] 关于接收后程序在向后执行的时间内，可能存在错过接收的可能性。recv函数是会有一个存储的过程的。应该是不会产生这种情况。
    确实存在该问题。

 - [ ] 向上接收模块的去留，疑似可以采集数据之后直接到数据库。有专门的函数模块。

## 后续工作

1. 增加log日志文件。
2. 优化错误处理。

## 对接

1. 数据不明确问题
2. 进行整体测试
3. 整体框架相互了解
