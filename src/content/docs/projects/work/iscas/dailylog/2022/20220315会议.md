---
title: "20220315会议"
description: "20220315会议"
---

# 20220315会议

明天接口讨论会时间9点半，在4号楼3层小会议室，初定讨论内容为：

上午集中讨论
1）标准体系讲解
     由孟博士负责，讲解标准体系框架，10所靳老师配合，制定文档模板，商讨确定分工
2）总体要求讨论
    由王维珅负责，过一遍总体要求文档，确定数据类型/方式/包头格式，补充缺少的内容
3）融合对接讨论：
    由翟文龙负责，按照靳老师、翟文龙、肖继文、连老师的顺序，过一遍接口协议文档，确定传输条件/频率/带宽，介绍关键字段，商讨控制指令/软件模型下发等具体事项

下午自由讨论
4）态势对接讨论：
    由连老师负责，根据上午数据介绍情况，与靳老师、翟文龙、肖继文对接态势显示需求，讨论可显示内容
5）数据库对接讨论
    由iTheds负责，与靳老师、翟文龙、肖继文对接船载数据库转发/入库/建表需求及方式
    由任老师负责，与靳老师、翟文龙、肖继文对接岸基数据与各系统分析/处理/标注软件接口

各位负责老师，麻烦记录下变动内容，最后汇总形成会议纪要

# 总体方案

声纳、无线电、雷达。

## 协议

传输协议

实时性要求
多少次 1 秒

船载多源信息网络存储服务器

NAS 数据库。
深海所服务器，NAS 硬盘 - JY。
入库的时候解析成指定格式。导出的时候指定格式。

北斗、哨兵？？？

哨兵是北斗+4G。比较少。
哨兵上不传北斗。
70 k 。

## 数据类型

8大类。

船载态势数据

融合数据库 is me。

侦察兵没有单独发。

哨兵如何引接数据库。
哨兵没有入库。

侦察兵接入数据库，使用原始 SQL 语句访问数据库。

## 具体格式

融合、远程控制。

各个系统各自维护自身状态。

支持协同的

# 船载数据库方

主要数据：
AIS
GPS
HDT
MWV
VLW
DBT
PLOT
TRACK

ADS-B

## 一、船载数据库转发功能

状态：
1. 目前可以支持响应 64 个用户进行获取数据。
2. 动态根据配置文件调整数据转发功能。
3. 记录转发数目，监听网络状态。

需求：
1. 约定的协议格式。

## 二、数据库存储

1. 使用数据库 mco 接口进行存储。

需求：
1. 数据存储到毫秒(ms)。
2. 目录入库。
3. 索引方式入库。
    Python， C++ ， RUST。

CSV 方式导入导出。

所有数据入库。
然后数据库导出，并且人工拷贝到子系统中，导入到各自子系统分系统。

## 三、远程访问服务

1. 支持主要的 ODBC 标准接口。
2. 支持使用 C++ 接口。

需求：
1. 约定数据库格式，进行访问。

## 三、数据库的文件导入导出

1. 支持 CSV 格式文件导入导出。
2. 支持 XML 格式文件导入导出。

## 四、数据格式、协议格式

十所-无线电(靳)、自动化-岸基(连、任渊)、声纳(张)、船载(王)

数据库原有的 8 种数据：
1. 数据格式：原生数据。
2. 存储表格式依据：待定。
3. 使用者：船载。

船载数据库存储数据，船载系统通过接口实时调用数据库内容。
人工拷贝到本地计算机之后，使用 mco 层访问数据库，或者使用 SQL 引擎 SQL92 访问数据库。

时间：

提供数据库接口，自己建表。

声纳 - python
1s一次

岸基 - Rust


无线电 - C++

10毫秒级录一次数据，可以慢一点。取决于数据的来源，100kb 左右一次。

船载
2.4s 一次。

### 远程访问服务

数据级别：
存储所有可能的数据。
尽可能大。

是否使用索引，索引规则

多读单写需求

联表查询和嵌套查询。

数据插入频率：

数据库的有限性问题，总会存满数据。

### 导入导出

CSV 样本。
通过 CSV 样本进行建表。

#
